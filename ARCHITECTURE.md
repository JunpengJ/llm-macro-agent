# 系统架构设计

> 本文档描述项目的整体架构与设计思路。核心评分规则、数据阈值、权重配比属于项目机密，不在本文档范围内。

---

## 一、设计目标

本系统用于**自动化宏观数据分析**，核心目标：

1. **自动化**：多源数据采集、结构化存储、报告生成全流程自动化
2. **可验证**：每一条输出都可追溯到数据源与规则依据
3. **抗幻觉**：通过工程化手段约束LLM输出，避免数据编造与逻辑跳步
4. **可迭代**：支持规则更新、版本归档、质量评估闭环
5. **可降级**：任一环节失败时系统能优雅降级，不产生错误输出

---

## 二、分层架构

系统采用**五层架构**，各层职责清晰、依赖单向：
```
┌─────────────────────────────────────────────┐
│  调度层（Scheduler）                         │
│  任务编排 · 异常检测 · 补充检索 · 输出管理     │
├─────────────────────────────────────────────┤
│  Agent层（Multi-Agent）                      │
│  搜索 · 核查 · 简报 · 分析（四类独立Agent）    │
├─────────────────────────────────────────────┤
│  校验层（Validation）                        │
│  六层幻觉防火墙 · 交叉核验 · 规则触发自检      │
├─────────────────────────────────────────────┤
│  记忆层（Memory）                            │
│  活跃记忆（动态基线）· 归档记忆（历史版本）     │
├─────────────────────────────────────────────┤
│  数据层（Data）                              │
│  多源采集 · 清洗结构化 · 快照/CSV/日历        │
└─────────────────────────────────────────────┘
```
**依赖方向**：调度层 → Agent层 → 校验层 → 记忆层 → 数据层。反向依赖严格禁止。

---

## 三、数据流
```
[多源数据采集]
    │
    ▼
[数据清洗与结构化] ──▶ [快照 / CSV / 日历]
    │
    ▼
[搜索Agent × N]（并行）
    │
    ▼
[事实核查Agent]
    │
    ▼
[简报Agent]
    │
    ▼
[六层幻觉防火墙]
    │
    ▼
[分析Agent]
    │
    ▼
[活跃记忆：动态基线]
    │
    ├──▶ [输出报告]
    │
    └──▶ [调度器：异常检测]
              │
              ├─ 正常 ──▶ [基线更新]
              │
              └─ 异常 ──▶ [补充搜索] ──▶ 重新进入核查流程
```

---

## 四、各层设计

### 4.1 数据层（Data Layer）

**职责**：采集、清洗、结构化存储多源数据。

**核心设计**：

- **多源采集**：支持 FRED、akshare、Sina 等数据源，官方公告与API并行
- **数据结构化**：统一字段命名、单位映射、时区对齐
- **本地快照**：每次采集生成时间戳快照，作为分析引擎的唯一权威数据源
- **单位映射表**：每个字段的标准单位在独立映射文件中维护，避免多模块单位换算不一致

**关键原则**：

- 快照数据为 S+ 级权威基准，所有分析以此为准
- 采集失败时保留上一份有效快照，不产生空数据
- 数据源变更时通过映射表切换，不修改业务代码

### 4.2 记忆层（Memory Layer）

**职责**：管理系统的长期状态，支持跨天、跨周、跨月的连续性。

**两类记忆**：

| 类型 | 作用 | 生命周期 |
|:---|:---|:---|
| **活跃记忆** | 每日动态基线，直接注入分析流程 | 每日更新 |
| **归档记忆** | 历史版本、废弃规则、历史案例 | 长期保存，按需检索 |

**核心设计**：

- **动态基线继承**：每日分析以昨日基线为起点，仅微调变化项，避免从零评估
- **版本归档机制**：规则迭代时旧版本不删除，移入归档记忆
- **按需检索**：遇到框架未覆盖的新情况时，检索归档记忆寻找历史处理方案
- **基线不可跨越**：新版本上线前，旧版本基线仍为当日唯一有效起点

### 4.3 校验层（Validation Layer）

**职责**：在LLM输出进入下游前，拦截幻觉、数据矛盾、逻辑跳步。

**六层防火墙**（设计思路见 `docs/hallucination-firewall.md`）：

1. 数据级校验：数值与快照逐项比对
2. 逻辑级校验：方向性常识检查
3. 信源级校验：分级标注与矛盾信号识别
4. 规则级校验：触发条件逐项自检
5. 交叉验证：多源信息互相印证
6. 熔断机制：异常时强制降级输出

**核心原则**：

- 模糊信息不得作为评分依据
- 矛盾信号必须标注，以权威源为准
- 方向性信号不得自行推断为触发

### 4.4 Agent层（Multi-Agent Layer）

**职责**：执行具体分析任务，每个Agent职责单一、可独立替换。

**四类Agent**：

| Agent | 输入 | 输出 | 特性 |
|:---|:---|:---|:---|
| **搜索Agent** | 关键词/任务 | 原始搜索结果 | 多实例并行，互不干扰 |
| **核查Agent** | 搜索结果 + 快照 | 分级情报包 | 每条情报标注可信度等级 |
| **简报Agent** | 核查结果 + 快照 | 结构化简报 | 只整合情报，不评分 |
| **分析Agent** | 简报 + 基线 | 分析报告 | 评分、预判、操作建议 |

**核心设计**：

- **职责单一**：每个Agent只做一件事，便于独立测试与替换
- **接口标准化**：Agent之间通过结构化文本传递，不依赖内部状态
- **降级机制**：任一Agent失败时，下游能使用上一环节的输出继续运行
- **可替换性**：模型升级时仅需修改 Agent Interface，不影响业务逻辑

### 4.5 调度层（Scheduler Layer）

**职责**：编排完整流程，处理异常，管理输出。

**核心设计**：

- **任务编排**：串行/并行混合，多搜索实例并行，后续流程串行
- **异常检测**：比对快照与前一基准，识别异常数据变动
- **补充检索**：异常无归因时自动触发补充搜索，二次核查后合并
- **降级链路**：任一环节失败时，明确降级路径，不产生错误输出
- **输出管理**：日志分级存储，中间产物可追溯

---

## 五、关键设计决策

### 5.1 为什么四层解耦（搜索/核查/简报/分析）

**背景**：早期版本将简报与评分耦合在同一模块，导致单点故障频发——简报格式变化即引发评分逻辑崩溃。

**决策**：拆分为四个独立环节，通过标准化接口传递。

**收益**：
- 任一环节可独立测试与替换
- 单点故障不再级联传播
- 各环节可独立优化提示词
- 可针对不同客户版本选择性启用

### 5.2 为什么引入动态基线（活跃记忆）

**背景**：每日从零评估会产生巨大波动，同一输入在不同时间可能给出不同结果。

**决策**：引入"动态基线继承"机制，每日以昨日基线为起点，仅微调变化项。

**收益**：
- 输出稳定性大幅提升
- 状态可追溯、可回放
- 避免LLM自由发挥

### 5.3 为什么设计分层记忆（活跃+归档）

**背景**：规则不断迭代，历史版本与废弃规则占用大量上下文，但完全删除又会导致"历史问题无法追溯"。

**决策**：将不常用的历史内容移入归档记忆，仅保留活跃规则在上下文中。

**收益**：
- 上下文效率优化
- 历史问题可回溯
- 裁剪不等于遗忘

### 5.4 为什么需要异常检测与补充检索

**背景**：真实场景中，重大事件往往先反映在数据上（如油价跳涨），新闻搜索存在滞后。

**决策**：设计自动异常检测，发现异常但无归因时触发补充检索。

**收益**：
- 不依赖单一搜索路径
- 重大事件及时捕捉
- 避免"数据异常被默默略过"

---

## 六、可扩展性

### 6.1 新增数据源

在数据层新增采集器，更新单位映射表即可，业务代码无需修改。

### 6.2 新增分析维度

在Agent层新增独立Agent，接口沿用标准格式。评分规则更新在框架文件，不涉及代码改动。

### 6.3 新增输出格式

在调度层新增输出模块，复用现有分析结果。例如日度、周度、月度报告可共享核心分析结果，仅输出格式不同。

### 6.4 多模型切换

Agent Interface 层抽象模型调用，切换底层LLM时仅修改接口适配，业务逻辑不变。

---

## 七、可靠性保障

| 保障维度 | 机制 |
|:---|:---|
| **数据可靠性** | 快照权威基准 + 多源交叉验证 + 单位映射表 |
| **逻辑可靠性** | 六层幻觉防火墙 + 规则触发自检 + 交叉核验 |
| **状态可靠性** | 动态基线继承 + 版本归档 + 状态可追溯 |
| **流程可靠性** | 四层解耦 + 降级链路 + 异常检测 + 补充检索 |
| **输出可靠性** | 标准化格式 + 核验表格 + 分级标注 |

---

## 八、目录结构
```
llm-macro-agent/
├── src/
│   ├── agent/              # Agent层实现
│   ├── utils/              # 通用工具
│   └── demo/               # 最小可运行示例
├── docs/                   # 设计文档
├── examples/               # 示例输出（合成数据）
└── README.md
```

---

## 九、相关文档

- [RAG全链路设计](docs/rag-pipeline.md)
- [六层幻觉防火墙](docs/hallucination-firewall.md)
- [分层记忆机制](docs/layered-memory.md)
- [异常检测与补充检索](docs/anomaly-detection.md)

---

**说明**：本文档仅描述架构设计思路与工程决策，不包含具体评分规则、阈值、权重、数据源清单等核心机密。
